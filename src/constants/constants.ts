export type ImportApi = {
    apiName?: string,
    apiVersion?: string,
    projectId?: string,
    apiSpecType?: string,
    schemaType?: string,
    content?: string,
    apiContentType?:string
  }